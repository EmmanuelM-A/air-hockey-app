package ema.mechanics;

/**
 * Holds the interface used to restart the game.
 */
public interface Restartable {
    /**
     * Restarts the game and all game components and values.
     */
    public void restart();
}
